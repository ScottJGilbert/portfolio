"use server";

export async function login(username: string, password: string) {
  //INPUTS MUST BE SANITIZED!!!!!!!!!!
  console.log(username);
  console.log(password);
}

export async function isLoggedIn(ip: number) {
  console.log(ip);
}
